<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#F3F3F3">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

    <Button
        android:layout_width="fill_parent"
        android:layout_height="80dp"
        android:drawableLeft="@drawable/compass"
        android:id="@+id/nearMeNowButton"
        android:drawablePadding="0dp"
        android:text="this is near me"></Button>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="80dp"
        android:drawableLeft="@drawable/compass"
        android:id="@+id/schedulerButton"
        android:drawablePadding="0dp"
        android:text="this is the scheduler"></Button>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="80dp"
        android:drawableLeft="@drawable/compass"
        android:id="@+id/SidurButton"
        android:drawablePadding="0dp"
        android:text="this is the sidur"></Button>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="80dp"
        android:drawableLeft="@drawable/compass"
        android:id="@+id/CompassButton"
        android:drawablePadding="0dp"
        android:text="this is the compass"></Button>

    </LinearLayout>
</RelativeLayout>